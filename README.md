Запит щоб знайти працівника з найменшою і найбільшою кількістю депозитних транзакцій після червня 2023.
1)Перший запит використовує один і той самий join трьох таблиць виконується 4 рази.
Немає тим часової таблиці з результатами , тому кожен раз сканує всю таблицю transactions
2) В другому за дпомогою CTE запит join виконується один раз і зберігає результат ,
так само другий CTE зберігає кількість транзакцій по кожному працівнику, вікона функція row_number() замість двох запитів як в першому прикладі  робить один,
за допомогою добавлених індексів можна звертатись по ним замість того щоб сканувати кожен раз таблицю по новому. 
ʕ ᵔᴥᵔ ʔ 
